import React from "react"
import { UserContext } from "../UserContext"



const User = () => {
  const { data } = React.useContext(UserContext);

  return (<section>

  </section>
  )
}

export default User;
